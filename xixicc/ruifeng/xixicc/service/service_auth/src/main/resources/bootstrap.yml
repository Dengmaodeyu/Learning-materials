#微服务配置
spring:
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0
  application:
    name: auth-service
  cloud:
    nacos:
      server-addr: 43.138.206.79:8848
      discovery:
        namespace: xixicc_dev
        group: xixicc-project
      config:
        namespace: xixicc_dev
        group: xixicc-project
        file-extension: yaml
        refresh-enabled: true
  #        shared-configs:
  #          - data-id: logging-${spring.profiles.active}.yaml
  #            group: xuecheng-plus-project
  #            refresh: true
  profiles:
    active: dev

mybatis-plus:
  mapper-locations: classpath:com/xixicc/service/*/mapper/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0

